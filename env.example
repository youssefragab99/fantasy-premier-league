# Database Configuration
POSTGRES_USER=fpl
POSTGRES_PASSWORD=fplpassword
POSTGRES_DB=fpldb
DB_HOST=db
DB_PORT=5432

# Override with full DATABASE_URL if needed
# DATABASE_URL=postgresql://fpl:fplpassword@db:5432/fpldb

# Data Loading Control
# Set to true to skip data loading (useful for development)
SKIP_DATA_LOADING=false

# Set to true to keep container running after data loading
KEEP_RUNNING=false

# Docker Volume Mount Mode (for development)
# MOUNT_MODE=rw